# Variables à copier dans Render ? Environment (PAS dans GitHub)
# Remplace les valeurs exemple par les tiennes ? JAMAIS de vrais secrets ici

DEBUG=False
SECRET_KEY=une-longue-cle-secrete-aleatoire
PYTHON_VERSION=3.13.2

ALLOWED_HOSTS=regiparc-1.onrender.com
CORS_ALLOWED_ORIGINS=https://regiparc-rglr.vercel.app
CSRF_TRUSTED_ORIGINS=https://regiparc-rglr.vercel.app

DB_NAME=defaultdb
DB_USER=avnadmin
DB_PASSWORD=votre_mot_de_passe_aiven
DB_HOST=votre-host.aivencloud.com
DB_PORT=18923
DB_SSL=True

# --- Email sur Render : utiliser Resend (SMTP Gmail est bloqué sur Render free) ---
# 1. Créer un compte gratuit : https://resend.com
# 2. API Keys ? Create API Key
# 3. Coller la clé ici / sur Render
RESEND_API_KEY=re_xxxxxxxx
# Pour tester sans domaine : onboarding@resend.dev (envoie surtout vers l'email du compte Resend)
# Ensuite tu peux vérifier ton domaine et mettre ton adresse
RESEND_FROM_EMAIL=RegiParc <onboarding@resend.dev>
DEFAULT_FROM_EMAIL=RegiParc <onboarding@resend.dev>

# SMTP Gmail = utile en LOCAL seulement (Render free = timeout port 587)
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_USE_TLS=True
# EMAIL_HOST_USER=votre.email@gmail.com
# EMAIL_HOST_PASSWORD=xxxx xxxx xxxx xxxx
